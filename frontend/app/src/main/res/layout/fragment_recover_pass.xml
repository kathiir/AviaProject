<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/jordy_blue"
    android:gravity="center"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:weightSum="10">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:layout_weight="8">

            <Button
                android:id="@+id/button_back"
                android:text="@string/title_recover_pass"
                style="@style/ButtonBackOnLoginScreens" />

            <EditText
                android:id="@+id/email_edit_text"
                style="@style/EditTextOnLoginScreen"
                android:autoLink="email"
                android:hint="@string/hint_email"
                android:inputType="textEmailAddress"
                android:autofillHints="email" />

            <Button
                android:id="@+id/button_register"
                style="@style/ButtonOnLoginScreens"
                android:text="@string/text_recover_pass" />

        </LinearLayout>
    </LinearLayout>
</LinearLayout>